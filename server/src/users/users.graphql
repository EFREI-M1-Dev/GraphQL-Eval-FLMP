type User {
  id: Int!
  username: String
  articles: [Article!]
  likes: [Like!]
}

input CreateUserInput {
  username: String
  password: String
}

type Query {
  users: [User]!
  user(username: String!): User
}

type Mutation {
  createUser(createUserInput: CreateUserInput!): User!
}
