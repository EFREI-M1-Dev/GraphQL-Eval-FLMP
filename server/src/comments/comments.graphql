type Comment {
  id: Int!
  text: String!
  createdAt: Timestamp
  author: User!
  article: Article!
}

type Query {
  comments: [Comment]!
  comment(id: Int!): Comment
}

type Mutation {
  createComment(articleId: Int!, text: String!): Comment!
  removeComment(id: Int!): Comment
}