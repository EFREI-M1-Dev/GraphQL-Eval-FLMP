query GetArticle($id: Int!) {
    article(id: $id) {
        title
        content
        image
        createdAt
        author {
            id
            username
            avatar
        }
    }
}
